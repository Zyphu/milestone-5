package model;

public class PlayerTurtle extends Turtle {
    public PlayerTurtle(Board board, Coordinate coordinate, Direction direction, Colour colour){
        super(board, coordinate, direction, colour);
    }
}
